# 退出码

pipguard 使用结构化退出码，方便脚本与 CI 判断结果：

| Code | 含义 | 触发场景 |
|------|------|----------|
| `0` | 通过 | 扫描结果可放行并安装成功 |
| `1` | 阻断 | 发现 HIGH/CRITICAL 风险 |
| `2` | 扫描错误 | 下载失败、格式不支持、文件异常等 |
| `130` | 用户中断 | 例如 Ctrl+C |

建议在 CI 中将 `1` 和 `2` 视为失败并停止后续步骤。
